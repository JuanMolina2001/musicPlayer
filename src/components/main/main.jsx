import Preview from './preview'

const Main = () => {
    return (
        <main className='flex flex-col items-center justify-center  bg-[var(--secondary)]'>
            <Preview/>
        </main>
    )
}

export default Main